package Examio;
/* 1바이트만 입력을 받음.
 * 몇개의 입력을 받을 건지 read로 선언해줘야 함.
 * 파일에서 1바이트를 읽어서 출력을 해주게 되는 것.
 * 읽어들일 데이터가 없으면(파일을 끝까지 읽었으면) -1이 들어옴.(EOF)
 * read 2가지 : 버퍼의 크기를 정하고 반환하는 값이 있고
 * 읽어들일 큰 단위 512
 * 보완해주는 클래스가 DataIn(out)putStream
 */